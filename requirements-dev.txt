# Test/CI dependencies. torch (CPU) is installed separately in CI so it doesn't
# pull the large CUDA build. Everything the services import + the test runner.
numpy==2.1.3
fastapi==0.115.6
uvicorn[standard]==0.32.1
redis==5.2.1
httpx==0.28.1
pytest==8.3.4
